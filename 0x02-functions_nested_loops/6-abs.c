#include "main.h"
/**
 * _abs - Returns the absolute value of an integer
 *
 * @a : integer number
 * Return: Always 0
 */
int _abs(int a)
{
	if (a >= 0)
	{
		return (a);
	}
	else
	{
		return (-1 * a);
	}
}
