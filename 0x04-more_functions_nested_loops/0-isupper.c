#include<stdio.h>
#include "main.h"
/**
 * _isupper - check if character is in upper case
 *
 * @c : ASCII code of a character
 * Return: Always 0
 */
int _isupper(int c)
{
	int i;

	if (c >= 65 && <= 90)
	{
		i = 0;

		return (i);
	}
	else
	{
		i = 1;

		return (i);
	}
}
